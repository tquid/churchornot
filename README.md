Church or Not examines a text address from a CSV file to check the returned `types` and `primaryType`. Would be neat if it actually looked for the church-type types,
but for now it just throws the data into a CSV to be fiddled with using good ol' spreadsheet tooling.
